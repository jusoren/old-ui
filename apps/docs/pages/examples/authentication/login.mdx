# Login

import { Tabs } from "nextra/components";
import { Button, Container, H1, P } from "ui";
import { FcGoogle } from "react-icons/fc";
import { GrGithub, GrMail } from "react-icons/gr";
import { TbMailFilled } from "react-icons/tb";

<Tabs items={['Preview', 'Code']}>
  <Tabs.Tab>
    <Container size="sm" className="py-7 text-center">
      <a href="#logoipsum" className="mx-auto mb-10">
        <img src="/logoipsum-1.svg" alt="Logoipsum" className="h-7" />
      </a>
      <H1 className="mb-2">Welcome Back</H1>
      <P dimmed>{`Let's continue your journey again`}</P>

      <div className="grid grid-cols-1 gap-5 mt-12">
        <Button fullWidth leftIcon={<FcGoogle size={20} />}>
          Continue with Google
        </Button>

        <Button fullWidth leftIcon={<img src="/microsoft.svg" className="w-4 h-4" />}>
          Continue with Microsoft
        </Button>

        <Button fullWidth leftIcon={<GrGithub size={20} />}>
          Continue with Github
        </Button>

        <Button fullWidth leftIcon={<GrMail size={20} />}>
          Continue with Email
        </Button>
      </div>
    </Container>

  </Tabs.Tab>
  <Tabs.Tab>
    ```js copy
    import { Button, Container, H1, P } from "@jusoren/ui";
    import { FcGoogle } from "react-icons/fc";
    import { GrGithub, GrMail } from "react-icons/gr";
    import { TbMailFilled } from "react-icons/tb";

    export function Login() {
      return (
        <Container size="sm" className="py-7 text-center">
          <a href="#logoipsum" className="mx-auto mb-10">
            <img src="/logoipsum-1.svg" alt="Logoipsum" className="h-7" />
          </a>
          <H1 className="mb-2">Welcome Back</H1>
          <P dimmed>{`Let's continue your journey again`}</P>

          <div className="grid grid-cols-1 gap-5 mt-12">
            <Button fullWidth leftIcon={<FcGoogle size={20} />}>
              Continue with Google
            </Button>

            <Button fullWidth leftIcon={<img src="/microsoft.svg" className="w-4 h-4" />}>
              Continue with Microsoft
            </Button>

            <Button fullWidth leftIcon={<GrGithub size={20} />}>
              Continue with Github
            </Button>

            <Button fullWidth leftIcon={<GrMail size={20} />}>
              Continue with Email
            </Button>
          </div>
        </Container>
      )
    }
    ```

  </Tabs.Tab>
</Tabs>
